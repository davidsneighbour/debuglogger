{
  "name": "@davidsneighbour/debuglogger",
  "description": "A customizable logging wrapper for debugging with log levels, external handlers, and console replacement.",
  "version": "0.0.1",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/davidsneighbour/debuglogger.git"
  },
  "author": "Patrick Kollitsch <patrick@davids-neighbour.com>",
  "keywords": [
    "debug",
    "logger",
    "console",
    "log-levels",
    "console-replacement"
  ],
  "dependencies": {
    "@eslint/js": "9.18.0",
    "eslint": "9.18.0",
    "prettier": "3.4.2",
    "rollup": "4.30.1",
    "vitest": "2.1.8"
  },
  "scripts": {
    "build": "rollup -c",
    "lint": "eslint src/ --fix",
    "release": "npm run test && npm run build && npm version patch -m 'Release %s' && git push && git push --tags && npm publish --access public",
    "test": "vitest run",
    "test:monitor": "vitest"
  },
  "main": "dist/DebugLogger.js",
  "files": [
    "dist"
  ],
  "module": "src/DebugLogger.js",
  "type": "module"
}
